<h1>My Events</h1>
<%= link_to "Home", e_index_path %>

<ul>
  <% @events.each do |event| %>
    <li>
      <h3><%= link_to "#{event.title}", e_path(event) %></h3>
      <%= render partial: "event_details", locals: { event: event } %>
    </li>
  <% end %>
</ul>